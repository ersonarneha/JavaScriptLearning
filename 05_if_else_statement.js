console.log(`Boomm boom`);
var isRoadBlocked=true;
if (isRoadBlocked) {
    console.log(`Traveling through :Lonavala`);
    console.log(`Traveling will be short just for 2 years`);
}
else{ console.log(`Traveling through Tunnel`);
console.log(`Traveling will be long just for 5 years`);
}